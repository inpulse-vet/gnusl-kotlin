rootProject.name = "gnusl-kotlin"

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include(":gnusl-binding")
include(":gnusl-kotlin")
